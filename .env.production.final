# Production Environment Variables (for VPS deployment)
# This configuration uses Docker bridge network IP to access the same database

# Database Configuration (Production - Docker bridge network)
DB_HOST=172.17.0.2
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=1qaz@WSX
DB_SSL=false

# Application Configuration
NODE_ENV=production
PORT=8002
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# CORS Configuration - Update with your domain
CORS_ORIGIN=http://your-vps-ip:8002

# Note: 
# - Development uses srv936559.hstgr.cloud (external DNS)
# - Production uses 172.17.0.2 (Docker bridge network IP)
# - Both connect to the same physical database instance